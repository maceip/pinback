# Pinback

Pinback is being rebuilt from zero as a durable DS4 cockpit.

The previous Python web wrapper, launchd supervisor, React bundle, assets, and tests were intentionally deleted after repeated demo failures.

Current state: architecture-only. No old wrapper code is retained.

See `docs/ARCHITECTURE_REDO.md`.
