<?xml version="1.0" encoding="utf-8"?>
<!-- pinback-server is HTTP by default (loopback, emulator 10.0.2.2, LAN, Tailscale). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
